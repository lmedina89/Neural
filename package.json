{
  "name": "micromind-learning-lab",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "npm test && node scripts/build.mjs",
    "benchmark": "node scripts/benchmark.mjs",
    "build": "node scripts/build.mjs"
  }
}
